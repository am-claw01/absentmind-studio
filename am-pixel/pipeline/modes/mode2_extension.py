"""
Mode 2 — Sprite Sheet Extension

CRITICAL RULES FOR THIS MODE:
1. Automated rubric gate 85/85 before human sees any new frame. Below 85 = full rebuild. [CONSTITUTION Rule 4]
2. DNA is already locked — new frames must match existing DNA; use dna_diff.py for continuity.
3. Call compliance gates before any commit that advances phase or locks data. [CHANGE-028]
4. Every frame in the extension is scored; weak frames block whole sheet approval.
5. Git commit convention per FOLDER_STRUCTURE.md after human approval.

See SPEC §5.2. Phase 0 stub.
"""

# Implementation pending — structure only.


def main() -> None:
    raise NotImplementedError("Mode 2 — Sprite sheet extension")


if __name__ == "__main__":
    main()
