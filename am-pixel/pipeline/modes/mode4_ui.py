"""
Mode 4 — UI Generation

CRITICAL RULES FOR THIS MODE:
1. UI sets evaluated as coherent families (icon grammar, frames) — not only single assets. [SPEC §5.4]
2. Automated gate 85/85 before human review for project-bound UI.
3. Palette and style must match locked UI direction; no drift from approved style selection.
4. DNA/project continuity applies to UI tied to a project; document exceptions in decision_log if any.
5. Mode 7 freeform does not apply — this mode is SNES/project scoped.

See SPEC §5.4. Phase 0 stub.
"""

# Implementation pending — structure only.


def main() -> None:
    raise NotImplementedError("Mode 4 — UI")


if __name__ == "__main__":
    main()
