"""Palette tokenizer — (palette_index, canvas_x, canvas_y) tuples. See am-pixel/SPEC.md and FOLDER_STRUCTURE.md. Phase 0 stub."""
# Implementation pending — structure only.


def main() -> None:
    raise NotImplementedError("Palette tokenizer — (palette_index, canvas_x, canvas_y) tuples")


if __name__ == "__main__":
    main()
